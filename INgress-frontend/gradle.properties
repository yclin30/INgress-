# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# 启用离线模式，因为Gradle 8.13已经成功安装
offline.mode=true
org.gradle.offline=true

# 增加Gradle堆内存，提高构建性能
org.gradle.jvmargs=-Xmx4096m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# 启用并行构建
org.gradle.parallel=true
# 启用守护进程
org.gradle.daemon=true
# 配置守护进程空闲超时时间（分钟）
org.gradle.daemon.idleTimeout=120
# 配置构建缓存
org.gradle.caching=true

# 配置HTTP连接超时和重试（增加超时时间和重试次数）
org.gradle.internal.http.connectionTimeout=120000
org.gradle.internal.http.socketTimeout=120000
org.gradle.internal.repository.max-retries=5
org.gradle.internal.repository.retry-delay=2000

# 配置系统网络代理和连接参数
systemProp.http.proxyHost=
systemProp.http.proxyPort=
systemProp.https.proxyHost=
systemProp.https.proxyPort=
systemProp.java.net.preferIPv4Stack=true
systemProp.jdk.http.auth.tunneling.disabledSchemes=

# 配置系统代理和镜像源
systemProp.https.proxyHost=
systemProp.https.proxyPort=

# 添加Maven镜像源配置
systemProp.repos.maven=https://mirrors.huaweicloud.com/repository/maven/
systemProp.repos.google=https://maven.aliyun.com/repository/google/
systemProp.repos.gradle=https://mirrors.aliyun.com/repository/gradle-plugin/

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true